name: Run Tests

on:
  push:
    branches: [ master, v2 ]
  pull_request:
    branches: [ master ]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: run tests
      run: |
        cd tests
        cat Makefile
        make test
